<div class="container">
  <div class="row">
    <div class="col-lg-10 col-xl-9 mx-auto">
      <div class="card card-signin flex-row  border-top-reviews">
        <div class="card-img-left d-none d-md-flex">
           <!-- Background image for card set in CSS! -->
        </div>
        <div class="card-body">
          <h5 class="card-title text-center text-primary">Create an account</h5>
          <%= form_for(resource, as: resource_name ,url: registration_path(resource_name))  do |f| %>
          <%= devise_error_messages! %>
            <div class="form-label-group">
              <%= f.text_field :name, autofocus: true, class: 'form-control', placeholder: "Full Name" %>
              <label for="name">Full Name</label>
            </div>

            <div class="form-label-group">
              <%= f.email_field :email, autofocus: false, class: 'form-control', placeholder: "Email Address" %>
              <label for="inputEmail">Email Address</label>
            </div>
            <div class="form-group ">
              <%= f.select :profile_type,['Individual', 'Business'],{}, autofocus: false, class: 'form-control select-css' %>
            </div>

            <div class="form-label-group">
              <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Password' %>
              <label for="password_field">Password</label>
            </div>

            <div class="form-label-group">
              <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control', placeholder: 'Confirm Password' %>
              <label for="password_confirmation">Confirm password</label>
            </div>

              <div class="form-label-group">
                <%= f.check_box :terms_of_service%> <small> <a href="#" data-toggle="modal" data-target="#myModal"> Terms & Conditions</a></small>
             </div>

            <%= f.submit "Sign up", class: "btn btn-primary btn-block btn-lg" %>
            <% end %>
            <div class="d-block text-center mt-2 small">
              <%= render "devise/shared/links" %>
            </div>

        </div>
      </div>
    </div>
  </div>
</div>


<!--pop up model-->


<!-- Modal -->
<div id="myModal" class="modal fade" role="dialog">
  <div class="modal-dialog">

    <!-- Modal content-->
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <small class="text-muted">By clicking above you agree to the @workglobal <a href="#">Terms of Use.</a>  Read our full Data Protection <a href="#">Policy here</a>. You may unsubscribe at any time from @workglobal emails and services.</small>
      </div>
    </div>

  </div>
</div>
